/*multiplication += k
                  var numberKeys = Object.keys(arrayChart[2][0]).length
                  total = multiplication / l.length / numberKeys
                  var total = parseFloat(total).toFixed(2)
                  //console.log(total, arrayChart)
                  $(".students").html(`${multiplication}`)
                  console.log(multiplication, total)*/
                  var numbers = arrayChart[2][0]
                  setTimeout(function(){
                    //myFunction()
                    multiplication.concat(numbers)
                    console.log(multiplication)
                  }, 1000)

                  /*function getSum(total, num) {
                      return total + num;
                  }*/
                  /*function myFunction(item) {
                      $(".students").html(numbers.reduce(getSum))
                      console.log(numbers.reduce(getSum))
                  }*/